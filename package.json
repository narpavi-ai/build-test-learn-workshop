{
  "name": "founder-starter-kit",
  "version": "1.0.0",
  "private": true,
  "description": "Founder Starter Kit — a brandable React + Vite front-end calling a Node/Express API over a local SQLite database. The skeleton you build your one magic-moment screen into.",
  "workspaces": [
    "apps/web",
    "apps/api"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c magenta,green \"npm run dev -w apps/api\" \"npm run dev -w apps/web\"",
    "dev:api": "npm run dev -w apps/api",
    "dev:web": "npm run dev -w apps/web",
    "seed": "npm run seed -w apps/api",
    "build": "npm run build -w apps/web"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "comments": {
    "overrides": "Force esbuild >=0.25 to clear GHSA-67mh-4wv8-2f99 (dev-server-only) without the breaking vite 5->8 jump that `npm audit fix --force` wants."
  },
  "overrides": {
    "esbuild": "^0.25.0"
  },
  "engines": {
    "node": ">=18"
  }
}
